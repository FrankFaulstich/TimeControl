# What building the documentation needs, on top of requirements.txt.
#
# Not on top of nothing: autodoc documents a module by importing it, so
# everything the application imports has to be installed as well, or the
# page comes out with the chapters missing and a warning nobody reads. That
# is issue #560, and it is why the docs job in CI installs both files.
sphinx
sphinx-rtd-theme
