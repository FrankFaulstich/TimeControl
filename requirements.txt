pyperclip
requests
packaging
streamlit
pywebview
spyne
lxml
fastapi
uvicorn
# TimeTrackerMCP_Server.py supports both mcp 1.x (FastMCP) and 2.x (MCPServer)
# via a version-detecting import - see its top-of-file comment - so no upper
# bound is needed here anymore.
mcp; python_version >= "3.10"